# DSH Zhipu Balance - 模型用量功能优化

## 功能概述

优化了 dsh-zhipu-balance 插件的模型用量显示功能，将模型用量直接集成到API额度板块内，提供更直观的使用体验。

## 🎯 功能改进

### 模型用量集成显示
- **位置**: 直接显示在API额度板块底部
- **功能**: 展示当前API使用的各个模型的名称和token使用量
- **格式**: 模型名称 + token使用量（使用万/亿单位显示）
- **分隔**: 使用分隔线与前面的API额度信息区分

### 设置选项
- **设置名称**: "显示模型用量"
- **类型**: 复选框
- **默认值**: 勾选（显示）
- **控制**: 用户可以选择是否显示模型用量信息

## 📋 技术实现

### 代码修改
1. **客户端代码** (`lib/client.js`):
   - 在 `readSettings()` 函数中添加了 `showModelUsage` 属性
   - 在设置面板中添加了"显示模型用量"复选框
   - 修改了API板块的渲染逻辑，将模型用量集成到API额度内
   - 添加了条件渲染，根据用户设置决定是否显示模型用量

2. **UI布局优化**:
   - 在API额度板块底部添加模型用量信息
   - 使用分隔线将模型用量与前面的API额度信息分开
   - 保持原有的折叠/展开功能

### 核心代码逻辑
```javascript
// 设置面板添加选项
React.createElement("div", { className: "dzb-setting-row" },
    React.createElement("span", { className: "dzb-setting-name" }, "显示模型用量"),
    React.createElement("input", {
        type: "checkbox",
        checked: settings.showModelUsage,
        onChange: function (e) {
            onChange(Object.assign({}, settings, { showModelUsage: e.target.checked }));
        }
    }))

// API板块集成模型用量
if (settings.showModelUsage && apiView.detail && apiView.detail.length > 0) {
    // 渲染模型用量信息
}
```

## 🚀 使用方法

1. 在DSH中启用 dsh-zhipu-balance 插件
2. 点击插件右上角的设置按钮（⚙）
3. 在设置中找到"显示模型用量"选项
4. 勾选该选项以显示模型用量信息
5. 模型用量会直接显示在API额度板块底部
6. 设置会自动保存，下次打开插件时会保持您的选择

## 📊 显示效果

### API额度板块结构
```
┌─────────────────────────────────────────────┐
│ 现金余额                                    │
│ ¥1,234.56                                  │
│                                            │
│ 资源包 · API 额度                           │
│ glm-4 套餐                                 │
│ 已用 45%                                   │
│ 剩 55,000 / 共 100,000 tokens              │
│                                            │
│ 账户额度                                   │
│ Token 额度                                 │
│ 已用 30%                                   │
│                                            │
│ ────────────────────────────────────────── │
│                                            │
│ 模型用量                                    │
│ glm-4     125,430 tokens                   │
│ glm-3-turbo 89,256 tokens                  │
│ glm-4v     45,123 tokens                   │
│ glm-4-airx 23,789 tokens                   │
└─────────────────────────────────────────────┘
```

## ✨ 优势

### 🎯 用户体验改进
- **信息集中**: 所有API相关信息集中在一个板块内
- **操作便捷**: 无需额外展开即可查看模型用量
- **界面整洁**: 减少板块数量，界面更加简洁

### 🔧 功能灵活性
- **灵活控制**: 用户可以根据需要选择是否显示
- **设置持久化**: 用户的显示偏好会自动保存
- **兼容性好**: 与现有功能完全兼容

### 📱 界面优化
- **视觉层次**: 使用分隔线清晰区分不同类型的信息
- **格式统一**: 保持与现有UI风格的一致性
- **响应式**: 适应不同屏幕尺寸

## 🔄 版本信息

- **功能版本**: v1.2.0
- **兼容性**: 与现有版本完全兼容
- **依赖**: 无额外依赖
- **向后兼容**: 不会影响现有用户的使用习惯

## 📝 注意事项

- 模型用量显示的是近24小时的使用数据
- 如果没有模型用量数据，该部分不会显示
- 用户可以通过设置选项控制是否显示此信息
- 数据会随着API的自动刷新而更新