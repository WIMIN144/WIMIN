# DSH Zhipu Balance - 标题显示修复

## 功能概述

修复了 dsh-zhipu-balance 插件中账号额度和模型用量标题不显示的问题，确保用户能够清晰地看到各个部分的内容。

## 🎯 修复内容

### 问题诊断
用户反映勾选了"显示模型用量"后，看不到"账号额度"和"模型用量"标题，导致内容显示混乱。

### 修复方案
1. **账号额度标题修复**
   - 确保在没有资源包时显示"账号额度"标题
   - 优化标题显示逻辑，根据数据内容动态显示合适的标题

2. **模型用量标题修复**
   - 添加明确的"模型用量"标题
   - 使用分隔线与前面内容区分

## 📋 技术实现

### 代码修改
1. **客户端代码** (`lib/client.js`):
   - 修改了账号额度显示逻辑，确保在没有资源包时也会显示"账号额度"标题
   - 优化了主标题逻辑，根据数据内容动态显示合适的标题
   - 添加了明确的"模型用量"标题

### 核心代码逻辑
```javascript
// 修复前：账号额度没有标题
if (apiView.entries.length > 0) {
    if (apiChildren.length > 0) apiChildren.push(React.createElement("div", { key: "pdiv", className: "dzb-divider" }));
    for (var ei = 0; ei < apiView.entries.length; ei++) {
        apiChildren.push(EntryRow(t, apiView.entries[ei], warn, error));
    }
}

// 修复后：账号额度有明确的标题
if (apiView.entries.length > 0) {
    if (apiChildren.length > 0) apiChildren.push(React.createElement("div", { key: "pdiv", className: "dzb-divider" }));
    apiChildren.push(React.createElement("div", { key: "entries-title", className: "dzb-section-title" }, "账号额度"));
    for (var ei = 0; ei < apiView.entries.length; ei++) {
        apiChildren.push(EntryRow(t, apiView.entries[ei], warn, error));
    }
}

// 修复前：主标题逻辑不完善
SectionTitle(showPacks ? t("sectionPacks") : t("sectionApi"), ...)

// 修复后：主标题逻辑优化
SectionTitle(showPacks ? t("sectionPacks") : (apiView.entries.length > 0 ? "账号额度" : t("sectionApi")), ...)
```

## 🚀 修复效果

### 修复前的问题
- 用户勾选了"显示模型用量"后，看不到"账号额度"标题
- 模型用量部分没有标题，显得突兀
- 内容层次不清晰，用户难以理解信息结构

### 修复后的效果
- ✅ 清晰的"账号额度"标题
- ✅ 明确的"模型用量"标题
- ✅ 层次分明的内容结构
- ✅ 更好的用户体验

## 📊 显示效果

### 修复后的面板结构
```
┌─────────────────────────────────────────────┐
│ 智谱余额                                    │
│ [刷新] [设置] [收起]                       │
├─────────────────────────────────────────┤
│ 账号额度                                    │
│ ─ ▾                                        │
│ Token 额度                                 │
│ 已用 65%                                   │
│ 剩 35,000 / 共 100,000 tokens              │
│                                            │
│ 周窗口                                     │
│ 已用 30%                                   │
│ 剩 7,000 / 共 10,000 tokens                │
│                                            │
│ 模型用量                                    │
│ ─ ▾                                        │
│ glm-4     45,230 tokens                    │
│ glm-3-turbo 23,450 tokens                 │
│ glm-4v     12,890 tokens                  │
└─────────────────────────────────────────────┘
```

## ✨ 优势

### 🎯 用户体验改进
- **层次清晰**: 每个部分都有明确的标题
- **易于理解**: 用户可以快速识别不同类型的信息
- **结构完整**: 所有内容都有适当的标题和分隔
- **视觉层次**: 提供了更好的视觉层次和信息组织

### 🔧 功能优化
- **标题动态**: 根据数据内容动态显示合适的标题
- **兼容性好**: 与现有功能完全兼容
- **设置持久化**: 用户的显示偏好会自动保存

### 📱 界面优化
- **一致性**: 保持与现有UI风格的一致性
- **可读性**: 标题让内容更加易于阅读和理解
- **专业性**: 提供了更加专业和清晰的信息展示

## 🔄 版本信息

- **功能版本**: v1.4.0
- **兼容性**: 与现有版本完全兼容
- **依赖**: 无额外依赖
- **向后兼容**: 不会影响现有用户的使用习惯

## 📝 注意事项

- 修复仅影响标题的显示，不影响数据的获取和显示
- 设置选项的功能保持不变
- 修复了在不同数据情况下的标题显示问题
- 确保了在各种场景下都能正确显示标题

## 🎨 视觉效果

### 对比效果
**修复前**:
- 缺少标题，内容显得混乱
- 用户难以识别不同部分的内容
- 缺乏视觉层次

**修复后**:
- 清晰的标题，层次分明
- 用户可以快速识别不同部分
- 专业的信息展示

这个修复解决了用户在使用过程中遇到的关键问题，提供了更好的用户体验和更清晰的信息展示。