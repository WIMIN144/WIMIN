# DSH Zhipu Balance - 白色背景功能

## 功能概述

为 dsh-zhipu-balance 插件添加了白色背景开关功能，解决在深色皮肤下插件内容看不清的问题。

## 新增功能

### 🎨 白色背景开关
- **位置**: 插件设置面板中
- **功能**: 启用后插件面板使用纯白色背景（#ffffff）
- **持久化**: 设置会保存在浏览器的localStorage中

## 技术实现

### 代码修改
1. **客户端代码** (`lib/client.js`):
   - 在 `readSettings()` 函数中添加了 `whiteBackground` 属性
   - 在CSS中添加了 `.dzb-white-bg` 类
   - 在设置面板中添加了复选框控件
   - 在面板渲染时根据设置动态添加白色背景类

2. **CSS样式**:
   ```css
   #dzb-panel.dzb-white-bg {
       background: #ffffff;
   }
   ```

### 设置选项
- **设置名称**: "白色背景"
- **类型**: 复选框
- **默认值**: 未勾选
- **重置**: 点击"恢复默认"按钮时会同时重置此选项

## 使用方法

1. 在DSH中启用 dsh-zhipu-balance 插件
2. 点击插件右上角的设置按钮（⚙）
3. 在设置中找到"白色背景"选项
4. 勾选该选项以启用纯白色背景
5. 设置会自动保存，下次打开插件时会保持您的选择

## 效果对比

### 默认主题背景
- 跟随DSH主题的背景色
- 在深色皮肤下可能看不清内容

### 白色背景
- 纯白色背景（#ffffff）
- 确保在任何皮肤下内容都清晰可见
- 特别适合深色主题用户

## 注意事项

- 此功能仅影响插件面板的背景色
- 不会影响其他UI元素或DSH主题
- 设置保存在浏览器本地，不同浏览器间的设置不互通

## 版本信息

- **功能版本**: v1.1.0
- **兼容性**: 与现有版本完全兼容
- **依赖**: 无额外依赖